# ============================================
# HITSTER Environment Variables
# ============================================
# Copy to .env and fill in values
# For Vercel: Add these in Project Settings > Environment Variables
# For Docker: Set in shell or .env file before running docker compose

# App URL (used for OAuth callbacks, QR codes)
# Vercel: Use your deployment URL (e.g., https://hitster.vercel.app)
# Docker: Use your server's public URL (e.g., https://hitster.example.com)
NEXT_PUBLIC_APP_URL=http://127.0.0.1:3000

# ============================================
# Database (PostgreSQL)
# ============================================
# Local dev: matches docker-compose.yml defaults (port 5433)
# Vercel: Use Vercel Postgres, Neon, or Supabase connection string
# Docker: Handled automatically in docker-compose.prod.yml
DATABASE_URL=postgresql://hitster:hitster@localhost:5433/hitster

# Docker production only: Set postgres password (default: hitster)
# POSTGRES_PASSWORD=your-secure-password

# ============================================
# Authentication
# ============================================
# Better Auth secret - min 32 characters
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-min-32-chars-here!

# ============================================
# Spotify OAuth
# ============================================
# Get credentials from https://developer.spotify.com/dashboard
# Add callback URL to Spotify Dashboard (use 127.0.0.1 for local dev, not localhost)
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret

# ============================================
# Build Options
# ============================================
# Skip env validation during build (not recommended for production)
# SKIP_ENV_VALIDATION=1
